<div class="col-md-6">
<pre><code class="ruby"># cat config.ru
require "roda"

class App < Roda
  route do |r|
    # GET / request
    r.root do
      r.redirect "/hello"
    end

    # /hello branch
    r.on "hello" do
      # Set variable for all routes in /hello branch
      @greeting = 'Hello'

      # GET /hello/world request
      r.get "world" do
        "#{@greeting} world!"
      end

      # /hello request
      r.is do
        # GET /hello request
        r.get do
          "#{@greeting}!"
        end

        # POST /hello request
        r.post do
          puts "Someone said #{@greeting}!"
          r.redirect
        end
      end
    end
  end
end

run App.freeze.app
</code></pre>
</div>

<div class="col-md-6">
  <div id="roda_tree">
    <h1 id="site_title">Roda</h1>
    <h2 id="site_subtitle">Routing Tree<br />Web Toolkit</h2>
  </div>
</div>

<p style="clear: both;">Roda is a routing tree web toolkit, with the primary goals of simplicity, reliability, extensibility, and performance.  Find out <a href="why.html">why you should use it</a>.</p>
